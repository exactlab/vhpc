# packages.yml - Runtime package installation configuration
# Mount this file to /packages.yml in containers to install additional packages

# Rocky Linux packages to install via dnf
dnf_packages:
  - htop
  - git
  - nano
  - vim
  - tree

# Python packages to install via pip
python_packages:
  - numpy
  - pandas
  - requests
  - matplotlib